# PWA Icons

Google Discover ve Android ana ekranı için PNG ikonlar ekleyin:

- `icon-192x192.png` – Android splash / home screen
- `icon-512x512.png` – PWA install / splash

Şu an manifest `public/icon.svg` kullanıyor. PNG ekledikten sonra `manifest.json` içindeki `icons` dizisini güncelleyin.
